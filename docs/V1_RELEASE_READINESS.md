# v1.0.0 发布验收进展 — 2026-09-10

2026-09-11 补充：候选 [PR #11](https://github.com/Dispel-wb/OpenAgentWorkbench/pull/11) 的提交 `4644e163b7c72cc9a155ccd3f8eab527f3ac959b` 已通过真实 CI 与 CodeQL；尚未合并、打标签或发布。后续提交必须重新检查。新增正式版证据门禁，24 个策略夹具通过不代表实际验收通过。Windows Sandbox 目前仅有[启用方案](WINDOWS_SANDBOX_PLAN.md)，未启用或重启；最终新候选仍需重新冻结并完成 72 小时验证。

**结论：尚未认证为可发布 v1.0.0。** 本轮确实修改了实现并运行真实进程测试；不以修改版本号替代验收。当前候选仍标识为 Preview。

## 本轮实际修复

1. Codex JSONL 改为读取时限长；坏 JSON 不再拼进回答冒充成功，单轮累计回答也有长度限制。
2. 后台 Host 启动异常不再弹出无人确认的模态框；记录错误并以失败码退出。旧候选在相同故障下 5 秒仍未退出，新候选约 0.4 秒退出。
3. 发布扫描拒绝 HashMismatch、NotTrusted 等异常签名；未签名保留明确警告，不伪造可信发行者。
4. 发布工作流在打包前检查同一提交的真实 CI，缺失、失败、未完成、其他提交或其他应用的结果均不算通过。
5. 依赖审查扩展到主分支推送；按用户授权启用仓库 Dependency graph 和 Dependabot 漏洞提醒。没有启用自动合并或发布。

## 已完成的验证

| 验证 | 结果与证据边界 |
| --- | --- |
| Codex 通信故障 | 2 轮正常对话 + 6 类异常/退出场景通过；包含坏 JSON、无换行超长输出、累计回答超限 |
| 签名策略 | 8 类签名状态断言通过；属于策略注入测试，不是可信签名证书验收 |
| 发布 CI 策略 | 8 类状态断言通过；对真实失败提交先拒绝，仓库修复并重跑 CI 后允许 |
| 真实 Codex CLI | 两轮、续接、用量映射通过；文件读取被核心策略拒绝并正确保留，**不是读取成功** |
| 真实 DSHarness CLI | 已锁定的 SDK Alpha：真实文件读取、两轮对话、中文和用量通过；旧无 SDK profile 入口不能使用 |
| 真实 Claude CLI + Host/MCP | scoped 工作区读写、越界拒绝、中文路径、多轮续接及跨父任务 DAG 通过 |
| 模型服务边界 | 上述真实 CLI 连接本机确定性模型服务，本轮没有付费云端模型调用；不冒充云端兼容认证 |
| 原生 Windows 本机矩阵 | Win11 build 26200、正常非管理员账户、Evergreen WebView2、中文空格路径、C 盘安装、全套运行通过；**不是干净 Win10 或物理无 D 盘机器** |
| 安装与持久化 | 安装、原子升级、previous 备份、卸载、SQLite WAL 与历史迁移测试通过 |
| 权限与扩展 | 工作区边界、危险命令审批、扩展隔离/重新隔离、MCP 校验、诊断脱敏通过 |
| 故障注入 | 100 次循环通过；已确认事件丢失 0，重复业务结果 0，自动恢复与 SQLite 完整性通过 |
| 文档界面 | 实际 Host + Chrome 的沙箱、布局、焦点、可访问对话框、工作流编辑器通过并检查截图；不替代读屏器人工验收 |
| 短期恢复 | 瞬时失败重试、休眠间隙识别、永久失败留证、Host 重启恢复及状态判定通过；不代替 72 小时测试 |
| 可复现构建 | 同机两次 OpenSource 构建字节一致，摘要见下方 |

冻结候选 SHA-256：`0C8D6B085AEE1AAF6525C5812D59017F77C0177BAD57F0DB81FB0306B9E3D1D0`。

首次受限环境的 DPAPI 初始化失败已在切换到正常 Windows 登录账户后重跑通过；没有关闭 DPAPI，也没有把失败结果覆盖成成功。

KISS 检测仍报告 AgentWorkerBridge 的分支密集状态机和历史隐式状态提示；保留已有协议结构，不为降低计数扩大无关重构。未将静态扫描当作完整安全审计。

## 远程 CI 与长期测试

- 仓库依赖审查曾因未启用依赖图失败。用户授权启用后，API 返回启用成功，依赖差异接口恢复；重跑的 [既有 CI](https://github.com/Dispel-wb/OpenAgentWorkbench/actions/runs/34373542609) 已成功。
- 该 CI 对应提交 `37e7eeaa32578e205d5ac73c846329b9daaa2543`，不是本轮所有本地修改。新候选仍需独立 PR 的远程 CI。
- 72 小时测试已在固定的候选 EXE 和测试脚本上启动：2026-09-10 20:38:55 至预计 2026-09-13 20:38:55，Asia/Shanghai，目标 25,920 个样本。当前是 **running，非 passed**。休眠、恢复或重试可能延长时间；完成后必须核验实际状态和摘要。
- 测试由独立后台任务运行，不占用聊天等待；不修改已安装程序、不调用付费模型。若修改候选程序，须重新冻结并重新计时。

## 仍阻止 v1.0.0 验收的条件

1. 完整 72 小时结果尚未产生。
2. 没有干净 Windows 测试机；仓库自托管 runner 数量为 0。不同系统、真实中文用户名、物理无 D 盘等仍未验收。
3. 尚缺正文至少十万字的真实会话导出与原生 WebView2 性能测量；当前检查到的工作台消息文件正文不足长度，不以文件字节数冒充正文量。
4. 完整读屏器/键盘人工验收、独立安全复核，以及不同核心审批/恢复能力边界仍待验收。
5. 最新本地修复还需候选 PR 的远程 CI 和最终发布资产核验。代码签名仍未完成；未签名发行须明确披露。

本清单不把完整 Office 编辑器列为必要条件，但简化预览限制必须在产品中明确。原有细项见 [成熟度清单](MATURITY_GAPS.md)。
