# 验证状态 — 2026-09-10

最新真实核心、原生 Windows 矩阵、故障注入和后台长期测试进度见 [v1.0.0 发布验收进展](V1_RELEASE_READINESS.md)。仍未认证为稳定版。

当前差距与验收条件见 [成熟度清单](MATURITY_GAPS.md)。最新候选包含 [文档预览取消修复](VALIDATION_PREVIEW_CANCELLATION.md)，以及 DSHarness 有界行读取与退出取消。DSHarness 的 7 个读取边界断言、正常两轮对话和 4 类传输故障夹具已通过；这是桥接器验证，不是新一轮真实云端模型认证。下列历史结果不自动覆盖最新候选。

最新本机变更与逐项验收见 [6.4.24 验收记录](VALIDATION_6.4.24.md)：付费模型验证、权限通道和越界修复、Office 图片预览。以下保留历史证据，不应把它们当作所有新功能均已完整验收。

这是候选验证记录，不是正式版完成证明。

## 已取得的证据

- Codex CLI 0.152.1：真实 CLI + 本机模拟 Responses 服务，两轮中文、resume、按轮用量通过。命令读取被核心策略拒绝，保留拒绝而未绕过；不计为文件执行通过。
- DSHarness 0.1.2-alpha.5 / Node 24.13.0：真实 CLI SDK + 本机模拟模型，两轮对话、真实 read 工具、中文空格路径和用量通过。旧版无 SDK profile 的入口被拒绝。
- 核心协议夹具：无 Provider 的 Codex 启动、失败/缺失结束事件、工作台记忆实际传入、未适配审批拒绝、DSHarness 核心丢失后不自动重放。
- 原有 Claude Worker、工作区长期记忆、权限、数据库迁移、安装升级/卸载、脱敏诊断与 UI contract 回归通过。
- API 适配器：HEAD 无正文、请求大小上限、错误正文/响应头/空闲超时、下游断开及停止任务取消上游、流截断拒绝、错误脱敏通过。
- 长期测试短夹具：瞬时错误有限重试、suspend/resume 间隙识别、永久故障证据与状态判定通过。不是 72 小时成功记录。
- Win11 build 26200 / Evergreen / x64 本机全套矩阵脚本通过。公共候选发布扫描通过；未签名。相同环境两次 OpenSource 构建摘要相同。

以上没有使用付费模型 API。可选 npm 运行时下载不等于模型调用。

## 仍需验收

- 干净 Win10/Win11、不同 Fixed Runtime、真实中文用户名、非管理员账户、物理无 D 盘机器及更多历史数据升级。
- DSHarness 跨进程会话恢复、两种新核心的工作台交互审批和实时改向；当前是明确拒绝或延迟队列，不是完整原生界面功能平移。
- 新的完整 72 小时后台稳定性通过记录、第三方 CLI 真实云端兼容性和独立安全审计。
- ApiServer / WorkbenchApi / app.js 仍需逐步拆分。KISS 检查提示大文件和分支密集；本轮提取 Worker bridge、Artifact、Provider catalog、UI Store，保留协议状态机的必要分支，不为降低静态计数大改业务。
- CI/CD 的远程执行结果必须在 Actions 中确认；本地脚本通过不能替代远程结果。Self-hosted 矩阵需要维护者提供隔离测试机。

没有可信代码签名证书，未使用自签名伪装可信发行者。候选现携带 1.0.0 元数据用于同字节长期测试，但尚未验收或发布为正式版。
